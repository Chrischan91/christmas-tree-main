name: Deploy Vite site to GitHub Pages

on:
  push:
    branches: ["main"]      # push 到 main 就自動部署
  workflow_dispatch:        # 也可以在 Actions 手動觸發

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20           # Node 20（穩定版）

      - name: Install dependencies
        run: npm install             # 安裝 package.json 裡的套件

      - name: Build with Vite
        run: npm run build           # 這一步會產生 dist/ 資料夾

      - name: Upload dist folder as Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist               # 只上傳 dist，而不是整個 repo

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
